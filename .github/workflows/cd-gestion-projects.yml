name: CD gestion-projects

on:
  workflow_run:
    workflows:
      - "CI gestion-projects"
    types:
      - completed
    branches:
      - main

jobs:
  cd:
    name: Deploy gestion-projects
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Authentification au Docker Registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build et Push de l'image Docker
        uses: docker/build-push-action@v5
        with:
          context: apps/backend/gestion-projects
          file: apps/backend/gestion-projects/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/smartsite-gestion-projects:latest
            ${{ secrets.DOCKER_USERNAME }}/smartsite-gestion-projects:${{ github.sha }}

      - name: Déploiement du conteneur
        run: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/smartsite-gestion-projects:latest
          docker stop smartsite-gestion-projects || true
          docker rm smartsite-gestion-projects || true
          docker run -d \
            --name smartsite-gestion-projects \
            --restart unless-stopped \
            -p 3007:3007 \
            -e PORT=3007 \
            -e NODE_ENV=production \
            -e MONGODB_URI=${{ secrets.MONGODB_URI }} \
            ${{ secrets.DOCKER_USERNAME }}/smartsite-gestion-projects:latest
          echo "✅ gestion-projects déployé sur le port 3007"
