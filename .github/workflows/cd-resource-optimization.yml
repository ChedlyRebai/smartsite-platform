name: CD resource-optimization

on:
  workflow_run:
    workflows:
      - 'CI resource-optimization'
    branches:
      - main
    types:
      - completed

jobs:
  cd:
    name: Deploy resource-optimization
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Connexion à Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build de l'image Docker
        run: |
          docker build \
            -t ${{ secrets.DOCKER_USERNAME }}/resource-optimization:latest \
            -t ${{ secrets.DOCKER_USERNAME }}/resource-optimization:${{ github.sha }} \
            apps/backend/resource-optimization

      - name: Push de l'image Docker
        run: |
          docker push ${{ secrets.DOCKER_USERNAME }}/resource-optimization:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/resource-optimization:${{ github.sha }}

      - name: Déploiement sur Kubernetes
        env:
          KUBECONFIG_DATA: ${{ secrets.KUBECONFIG }}
        run: |
          echo "$KUBECONFIG_DATA" | base64 -d > kubeconfig.yaml
          export KUBECONFIG=kubeconfig.yaml
          kubectl set image deployment/resource-optimization \
            resource-optimization=${{ secrets.DOCKER_USERNAME }}/resource-optimization:${{ github.sha }} \
            -n smartsite
          kubectl rollout status deployment/resource-optimization -n smartsite

      - name: Confirmation du déploiement
        run: |
          echo "✅ resource-optimization déployé avec le tag ${{ github.sha }}"
